# Private review data

Real decisions and source locators are not distributed. Keep your own datasets under the ignored `.radar/feedback/` directory. The test suite and `radar demo` use invented examples.
